name: Main Branch CI
on:
  push:
    branches:
      - main # This only triggers AFTER a merge, not during the PR.

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI running on main"